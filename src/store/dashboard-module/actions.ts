import { ActionTree } from 'vuex'
import { IDashboardState } from './state'
import { StateInterface } from '../index'
// import { NetworkServiceResponse } from '@/models/networkServiceResponse'
import axios, { AxiosResponse } from 'axios'
// import { awsBaseURL, apiKey, netsuiteBaseURL } from '@/libs/axios'

// Actions are asynchronous
const actions: ActionTree<IDashboardState, StateInterface> = {
  // someAction(/* { commit }, payload  */) {
  //   // a line to prevent linter errors
  // }
}

export default actions
